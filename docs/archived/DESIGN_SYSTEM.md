# Lago 设计系统

## 🎨 设计原则

### 1. 颜色限制
- **功能色限制**: 每个页面最多使用 6 种功能色
- **背景色限制**: 每个页面最多使用 4 种背景色
- **统一性**: 所有组件遵循相同的颜色规范

### 2. 按钮优先级
- **主按钮**: 使用渐变色，用于最重要的操作（如创建、提交）
- **次按钮**: 使用纯色，按优先级递减变浅
- **文本按钮**: 使用中性色，用于次要操作

## 🎯 6种功能色系统

### 1. 主色调 (Primary) - 蓝色系
```css
primary-50:  #E3F2FD  /* 浅蓝背景 */
primary-100: #BBDEFB  /* 浅蓝边框 */
primary-500: #2196F3  /* 主色 */
primary-600: #1976D2  /* 主色深 */
primary-700: #1565C0  /* 主色更深 */
```

### 2. 成功色 (Success) - 绿色系
```css
success-50:  #E8F5E8  /* 浅绿背景 */
success-100: #C8E6C9  /* 浅绿边框 */
success-500: #4CAF50  /* 成功色 */
success-600: #43A047  /* 成功色深 */
success-700: #388E3C  /* 成功色更深 */
```

### 3. 警告色 (Warning) - 橙色系
```css
warning-50:  #FFF8E1  /* 浅黄背景 */
warning-100: #FFECB3  /* 浅黄边框 */
warning-500: #FF9800  /* 警告色 */
warning-600: #FB8C00  /* 警告色深 */
warning-700: #F57C00  /* 警告色更深 */
```

### 4. 错误色 (Error) - 红色系
```css
error-50:  #FFEBEE  /* 浅红背景 */
error-100: #FFCDD2  /* 浅红边框 */
error-500: #F44336  /* 错误色 */
error-600: #E53935  /* 错误色深 */
error-700: #D32F2F  /* 错误色更深 */
```

### 5. 信息色 (Info) - 青色系
```css
info-50:  #E1F5FE  /* 浅青背景 */
info-100: #B3E5FC  /* 浅青边框 */
info-500: #00BCD4  /* 信息色 */
info-600: #00ACC1  /* 信息色深 */
info-700: #0097A7  /* 信息色更深 */
```

### 6. 中性色 (Neutral) - 灰色系
```css
neutral-50:  #FAFAFA  /* 浅灰背景 */
neutral-100: #F5F5F5  /* 浅灰边框 */
neutral-200: #EEEEEE  /* 边框 */
neutral-300: #E0E0E0  /* 边框深 */
neutral-500: #9E9E9E  /* 次要文本 */
neutral-600: #757575  /* 次要文本深 */
neutral-700: #616161  /* 主要文本 */
neutral-900: #212121  /* 主要文本深 */
```

## 🎨 渐变背景

### 主按钮渐变（更柔和、符合孩子审美）
```css
gradient-primary: linear-gradient(135deg, #64B5F6 0%, #42A5F5 100%)
gradient-success: linear-gradient(135deg, #81C784 0%, #66BB6A 100%)
gradient-warning: linear-gradient(135deg, #FFB74D 0%, #FFA726 100%)
gradient-error:   linear-gradient(135deg, #EF5350 0%, #E57373 100%)
gradient-info:    linear-gradient(135deg, #4FC3F7 0%, #29B6F6 100%)
```

### 儿童友好渐变（专为孩子设计）
```css
gradient-kid-blue:   linear-gradient(135deg, #81D4FA 0%, #4FC3F7 100%)
gradient-kid-green:  linear-gradient(135deg, #A5D6A7 0%, #81C784 100%)
gradient-kid-purple: linear-gradient(135deg, #CE93D8 0%, #BA68C8 100%)
gradient-kid-orange: linear-gradient(135deg, #FFCC80 0%, #FFB74D 100%)
gradient-kid-pink:   linear-gradient(135deg, #F8BBD9 0%, #F48FB1 100%)
```

## 📝 使用规范

### 按钮优先级

#### 1. 主按钮（Primary Button）
- **用途**: 最重要的操作（创建、提交、确认）
- **样式**: 使用渐变色 `bg-gradient-primary`
- **示例**: 提交表单、创建内容、确认操作

```tsx
<button className="bg-gradient-primary text-white px-6 py-3 rounded-lg">
  创建
</button>
```

#### 2. 次按钮（Secondary Button）
- **用途**: 重要但不主要的操作
- **样式**: 使用纯色 `bg-primary-500`
- **示例**: 编辑、保存、下一步

```tsx
<button className="bg-primary-500 text-white px-4 py-2 rounded-lg">
  保存
</button>
```

#### 3. 三级按钮（Tertiary Button）
- **用途**: 一般操作
- **样式**: 使用浅色 `bg-primary-100 text-primary-700`
- **示例**: 查看详情、设置

```tsx
<button className="bg-primary-100 text-primary-700 px-4 py-2 rounded-lg">
  查看详情
</button>
```

#### 4. 文本按钮（Text Button）
- **用途**: 次要操作
- **样式**: 使用中性色 `text-neutral-600 hover:text-neutral-700`
- **示例**: 取消、返回、更多

```tsx
<button className="text-neutral-600 hover:text-neutral-700 px-2 py-1">
  取消
</button>
```

### 背景色使用

#### 页面背景
- **主背景**: `bg-background-primary` (白色)
- **次背景**: `bg-background-secondary` (浅灰)
- **功能背景**: `bg-primary-50` 或 `bg-success-50`

#### 卡片背景
- **卡片**: `bg-background-primary border border-neutral-200`
- **强调卡片**: `bg-primary-50 border border-primary-100`

### 文本颜色

#### 文本层级
- **主文本**: `text-neutral-900` 或 `text-neutral-700`
- **次文本**: `text-neutral-600`
- **辅助文本**: `text-neutral-500`

## 🧒 儿童友好设计指导

### 1. 颜色选择原则
- ✅ 使用柔和、温暖的色调
- ✅ 避免过于鲜艳或刺眼的颜色
- ✅ 优先使用 400 色阶而非 500 色阶
- ✅ 背景使用半透明效果增加层次感

### 2. 卡片设计
- ✅ 使用 `bg-white/80` 或 `bg-white/90` 增加柔和感
- ✅ 添加 `backdrop-blur-sm` 增加毛玻璃效果
- ✅ 边框使用更浅的颜色如 `border-purple-100/50`
- ✅ 阴影使用 `shadow-lg` 而非 `shadow-xl`

### 3. 按钮设计
- ✅ 主按钮使用儿童友好渐变
- ✅ 避免过于强烈的对比度
- ✅ 使用圆角设计增加亲和力

## 🚫 避免的做法

### 1. 颜色滥用
- ❌ 不要在一个页面使用超过 6 种功能色
- ❌ 不要在一个页面使用超过 4 种背景色
- ❌ 不要随意使用渐变色，仅用于主按钮
- ❌ 不要使用过于鲜艳的颜色（如 500+ 色阶）

### 2. 按钮混乱
- ❌ 不要所有按钮都使用渐变色
- ❌ 不要按钮颜色没有优先级区分
- ❌ 不要使用过于鲜艳或对比度不足的颜色

### 3. 背景混乱
- ❌ 不要使用过多的装饰性背景
- ❌ 不要背景色与内容色冲突
- ❌ 不要使用过于复杂的渐变背景
- ❌ 不要使用完全不透明的白色背景

## ✅ 最佳实践

### 1. 颜色选择
- ✅ 优先使用设计系统中的标准颜色
- ✅ 保持颜色的一致性和可预测性
- ✅ 考虑颜色对可访问性的影响

### 2. 按钮设计
- ✅ 明确按钮的优先级和用途
- ✅ 使用合适的颜色表达按钮的重要性
- ✅ 保持按钮样式的统一性

### 3. 背景设计
- ✅ 使用简洁的背景，突出内容
- ✅ 合理使用背景色创建视觉层次
- ✅ 保持背景与前景的对比度

## 📱 组件示例

### BottomNavigation 组件
```tsx
// 主按钮 - 使用渐变色
className="bg-gradient-primary text-white"

// 次按钮 - 使用纯色
className="bg-primary-500 text-white"

// 三级按钮 - 使用浅色
className="bg-primary-100 text-primary-700"

// 文本按钮 - 使用中性色
className="text-neutral-600 hover:text-neutral-700"
```

### 快速操作面板
```tsx
// 主操作 - 随身记
className="bg-gradient-primary"

// 次操作 - 作品集
className="bg-gradient-success"

// 三级操作 - 查作业
className="bg-gradient-info"
```

## 🔄 迁移指南

### 从旧颜色迁移
```css
/* 旧颜色 -> 新颜色 */
primary-coral -> primary-500
primary-mint -> success-500
accent-blue -> primary-500
accent-green -> success-500
text-gray-* -> text-neutral-*
bg-gray-* -> bg-neutral-*
```

### 渐进式迁移
1. 新组件直接使用新设计系统
2. 修改现有组件时逐步迁移
3. 保持向后兼容，旧颜色名称映射到新颜色
4. 最终移除所有旧颜色引用

---

**更新时间**: 2025-01-10  
**版本**: v2.0  
**维护者**: Lago 设计团队
