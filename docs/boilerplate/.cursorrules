# [PROJECT_NAME] 项目 AI Context 规则

## ⚠️ 包管理器说明

**本项目使用 npm 进行依赖管理和构建，不使用 pnpm 或 yarn。**

- 所有安装命令必须使用 `npm install` 或 `npm install -D`
- 所有构建命令必须使用 `npm run build`
- 不要在项目中使用 `pnpm` 或 `yarn` 命令
- 当用户需要安装依赖时，必须使用 `npm install`

## 📚 核心文档作为Context

当处理 [PROJECT_NAME] 项目的任何开发问题时，请按以下优先级阅读文档：

### 1. 必读核心文档 (优先级从高到低)

1. **docs/DEVELOPMENT_GUIDE.md** ⭐⭐⭐⭐⭐
   - 最核心的开发指南
   - 包含API开发、数据库迁移、文档规范
   - 适用于所有开发任务

2. **docs/DATABASE_GUIDE.md** ⭐⭐⭐⭐⭐
   - 完整的数据库结构说明
   - 数据模型和关系
   - 适用于涉及数据库的任务

3. **docs/ARCHITECTURE.md** ⭐⭐⭐⭐⭐
   - 系统架构设计
   - 技术栈和模块划分
   - 适用于架构相关问题

4. **docs/PROJECT_STRUCTURE.md** ⭐⭐⭐⭐
   - 项目目录结构
   - 文件组织规范
   - 适用于项目初始化

5. **docs/API_DEVELOPMENT.md** ⭐⭐⭐⭐
   - API开发详细规范
   - Swagger配置和使用
   - 适用于API开发任务

6. **docs/DESIGN_SYSTEM.md** ⭐⭐⭐
   - 设计系统分析和配置指南
   - 如何从项目文档提取设计系统
   - Tailwind CSS 配置
   - 适用于UI开发任务

### 2. 文档组合使用策略

根据任务类型，组合阅读相应文档：

| 任务类型 | 推荐文档组合 |
|---------|-------------|
| 开发新API | DEVELOPMENT_GUIDE + API_DEVELOPMENT + DATABASE_GUIDE |
| 开发新页面 | DEVELOPMENT_GUIDE + DESIGN_SYSTEM |
| 数据库迁移 | DATABASE_GUIDE + DEVELOPMENT_GUIDE |
| 架构重构 | ARCHITECTURE + DATABASE_GUIDE |
| UI修改 | DESIGN_SYSTEM + DEVELOPMENT_GUIDE |
| 项目初始化 | PROJECT_STRUCTURE + ARCHITECTURE |

## 🎯 开发流程规范

### API开发

1. 在 `src/config/swagger.ts` 定义 Swagger Schema（用于 $ref 引用）
2. 在 `src/routes/` 定义路由和 Swagger 描述（使用 $ref 引用 Schema）
3. 在 `src/schemas/` 使用 class-validator 定义验证 Schema
4. 在 `src/controllers/` 实现控制器逻辑
5. 在 `src/services/` 实现业务服务

### 数据库变更

1. 修改 `prisma/schema.prisma`
2. 运行 `npx prisma db push`（开发）或 `npx prisma migrate dev`（生产）
3. 在 `src/scripts/seed.ts` 中处理存量数据迁移和种子数据更新

### 前端开发

1. **先分析项目设计文档**：从 `docs/DESIGN_SYSTEM.md` 学习如何提取设计系统
2. **配置设计系统**：将从设计文档提取的信息配置到 `tailwind.config.js`
3. 在 `app/` 目录创建页面（Next.js App Router）
4. 在 `components/` 创建可复用组件
5. 在 `lib/` 创建工具函数和 API 客户端
6. 使用 Tailwind 设计系统类名（从设计文档提取并配置）

### 文档管理

- 临时总结: 放在 `docs/WIP/`
- 完成后: 更新核心文档，总结移至 `docs/archived/`
- 测试文件: 放在 `test/`

## 🚫 禁止事项

- **不要使用 pnpm 或 yarn**，必须使用 npm
- 不要直接执行SQL，永远使用Prisma
- 不要在根目录创建测试文件
- 不要创建重复的功能总结文档
- 不要跳过 API 生成步骤
- 不要在路由注释中重复定义 Schema，使用 $ref 引用

## ✅ 最佳实践

- API接口必须有完整的Swagger文档
- 所有代码必须有TypeScript类型定义
- 数据库变更必须通过Prisma Schema
- 新功能必须更新对应的核心文档
- Swagger Schema 定义在 `src/config/swagger.ts`，通过 $ref 引用
- **设计系统必须从项目设计文档中提取**，配置到 `tailwind.config.js` 中
- 使用 npm 安装依赖，不要使用 pnpm 或 yarn

## 📂 项目关键路径

```
# 后端
apps/[project]-server/
├── src/routes/        # API路由定义 + Swagger（使用 $ref）
├── src/schemas/       # 验证Schema (class-validator)
├── src/controllers/   # 控制器
├── src/services/      # 业务服务
├── src/config/swagger.ts # Swagger Schema定义（用于 $ref）
└── prisma/schema.prisma # 数据库Schema

# 前端
apps/[project]-app/
├── app/              # Next.js页面 (App Router)
├── components/       # 可复用组件
├── lib/             # 工具库（API调用等）
└── tailwind.config.js # 设计系统配置

# 文档
docs/
├── README.md                # 文档导航
├── DEVELOPMENT_GUIDE.md     # 开发指南 ⭐⭐⭐⭐⭐
├── ARCHITECTURE.md          # 架构设计 ⭐⭐⭐⭐⭐
├── DATABASE_GUIDE.md        # 数据库指南 ⭐⭐⭐⭐⭐
├── API_DEVELOPMENT.md       # API开发规范 ⭐⭐⭐⭐
├── PROJECT_STRUCTURE.md     # 项目结构 ⭐⭐⭐⭐
└── DESIGN_SYSTEM.md         # 设计系统 ⭐⭐⭐
```

## 🔧 常用命令

> **⚠️ 重要**: 所有命令必须使用 **npm**，不要使用 pnpm 或 yarn。

```bash
# 安装依赖（使用 npm）
npm install

# 启动开发（使用 npm）
npm run dev

# 数据库操作
cd apps/[project]-server
npm run db:push
npm run db:seed
npx prisma studio

# 生成API
cd apps/[project]-app
# 使用 Swagger 文档生成 API 客户端代码
```

## 📝 记住

- 核心文档是项目知识的唯一来源
- 保持文档与代码同步
- 遵循既定的开发流程
- 所有变更都要有文档记录
- 使用 npm，不要使用 pnpm 或 yarn
- Swagger Schema 定义在 `src/config/swagger.ts`，通过 $ref 引用
- 设计系统配置在 `tailwind.config.js` 中

